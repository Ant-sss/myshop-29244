<%= render 'shared/header' %>
<div class="top">
<div class="main-left">
</div>
<div class="main-right">
  <ul class="shops">
  <% @shops.each do |shop| %>
    <li class="shops-list">
      <%= link_to shop_path(shop.id), class:"shops-link" do %>
        <div class="shop-name">
          <%= shop.name %>
        </div>
        <%= image_tag shop.image, class: "shop-image" %>
        <div class="shop-text">
          <%= shop.text %>
        </div>
        
      <% end %>
    </li>
  <% end %>
  </ul>
</div>
</div>

